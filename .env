# ─── Puertos expuestos en el host ─────────────────────────────────────────────
BACKEND_PORT=5000
FRONTEND_PORT=3000

# ─── URL del backend (se hornea en el bundle del frontend al hacer build) ─────
NEXT_PUBLIC_API_URL=http://localhost:5000/api/v1
